spring.application.name=dtem

# 서버 포트 설정 (Railway 자동 할당)
server.port=${PORT:8080}

# 데이터베이스 설정 (Railway PostgreSQL)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/dtem}
spring.datasource.driver-class-name=org.postgresql.Driver
# Railway는 DATABASE_URL에 username/password 포함되어 있음
spring.sql.init.mode=never
spring.jpa.hibernate.ddl-auto=update

# JPA 설정 (PostgreSQL)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# SEQUENCE 설정
spring.jpa.properties.hibernate.id.new_generator_mappings=true

# 타임스탬프 설정
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Seoul

# 로깅 설정
logging.level.root=INFO
logging.level.com.example.dtem=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE
logging.level.org.springframework.boot.autoconfigure=ERROR

# 메일 발송 설정 (Gmail 사용)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kanarazusann@gmail.com
spring.mail.password=ncjh rzdf mvug nlef
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# 메일 발송자 정보
mail.from.address=noreply@onultem.com
mail.from.name=오늘득템

# 파일 업로드 설정
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload.dir=src/main/resources/static/uploads/posts
file.upload.profile.dir=src/main/resources/static/uploads/profiles


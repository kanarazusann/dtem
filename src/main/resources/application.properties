spring.application.name=dtem

# 서버 포트 설정 (Railway 자동 할당)
server.port=${PORT:8080}

# 데이터베이스 설정 (프로파일별로 자동 전환)
# Railway 배포 시 DATABASE_URL 환경변수를 자동으로 사용합니다
spring.datasource.url=${DATABASE_URL:jdbc:oracle:thin:@localhost:1521:xe}
spring.datasource.driver-class-name=${DB_DRIVER:oracle.jdbc.OracleDriver}
spring.datasource.username=${DB_USERNAME:C##jspexam}
spring.datasource.password=${DB_PASSWORD:m1234}
spring.sql.init.mode=never
spring.jpa.hibernate.ddl-auto=update

# JPA 설정
spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.OracleDialect}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Oracle SEQUENCE 설정
spring.jpa.properties.hibernate.id.new_generator_mappings=true

# 타임스탬프 설정
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Seoul

# 로깅 설정
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# 메일 발송 설정 (Gmail 사용)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kanarazusann@gmail.com
spring.mail.password=ncjh rzdf mvug nlef
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# 메일 발송자 정보
mail.from.address=noreply@onultem.com
mail.from.name=오늘득템

# 파일 업로드 설정
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload.dir=src/main/resources/static/uploads/posts
file.upload.profile.dir=src/main/resources/static/uploads/profiles

